name: create runner

on: workflow_dispatch

jobs:
  create-runner:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: list files
        run: |
          pwd
          ls -la
          ls -R